# Dependencies for Deep Research Agent
# Create a virtualenv and install with:
#   python -m venv .venv && .venv\Scripts\activate  (Windows)
#   pip install -r python/requirements-deep-research.txt

# Core LangChain + LangGraph
langchain>=0.2.14
langchain-core>=0.2.35
langgraph>=0.2.28

# Groq provider for LangChain
langchain-groq>=0.1.9
# Optional but recommended (some versions may require it)
groq>=0.5.0

# Tavily search client (import name: tavily)
tavily-python>=0.4.0

# Supporting deps
pydantic>=2.7.0
typing_extensions>=4.10.0
nest_asyncio>=1.6.0

# If you hit SSL or HTTP issues, you may need to ensure these are up-to-date
httpx>=0.27.0
requests>=2.31.0
